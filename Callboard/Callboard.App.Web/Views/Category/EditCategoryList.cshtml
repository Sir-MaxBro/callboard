@model IReadOnlyCollection<Category>

<div class="row">
    <div class="col s12">
        @foreach (var category in Model)
        {
            Html.RenderPartial("EditCategory", category);
        }
    </div>
</div>