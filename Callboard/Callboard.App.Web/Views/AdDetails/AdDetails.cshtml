@model AdDetails

<div>
    @Model.Name
</div>
<div>
    @Model.Price
</div>
<div>
    @Model.Description
</div>
<div>
    @Model.AddressLine
</div>

@{ Html.RenderAction("GetLocationByCityId", "Location", new { cityId = Model.CityId }); }

@{ Html.RenderAction("GetUser", "User", new { userId = Model.UserId }); }