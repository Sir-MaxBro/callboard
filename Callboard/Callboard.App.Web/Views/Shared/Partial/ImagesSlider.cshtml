@model ImageViewModel

<div class="carousel carousel-slider">
    @foreach (var item in Model.Images)
    {
        <a class="carousel-item" href="#@item.ImageId">
            @Html.RenderImage(item.Data, item.Extension, "ad__image_big ad__image-carousel")
        </a>
    }
</div>